inversao de controle: spring é um grande map de beans, ele resolve o problema de ordem de dependencias
não usar metodos static
controller é burro, delega tudo (service, repository)
xml papapa
objeto dto
data jdbc mais baixo nível que hybernate (data jpa)

ajeitar redirecionamento pra confirmacao //check
configurar banco de dados //check

modelview (objeto pra injetar valores num template) (dto)
-uma página para editar cadastro
-hash (basicamente convenção de url.)
-get/new: index.html; post: redirect:pag listar; get listar; botão edit (PUT.html/UPDATE.sql)